# 효과음 시스템 구현 완료

## 구현 내용

### 1. Sound 모듈 생성 (`js/sound.js`)
- 효과음 재생 및 관리 시스템
- Web Audio API를 사용한 합성 효과음 생성
- MP3 파일이 있으면 자동으로 우선 사용
- localStorage에 설정 자동 저장

#### 주요 기능
- `Sound.play(soundName)`: 효과음 재생
- `Sound.setVolume(value)`: 볼륨 조절 (0.0 - 1.0)
- `Sound.toggleMute()`: 음소거 토글
- `Sound.setMute(muted)`: 음소거 설정

#### 효과음 종류
| 효과음 | 재생 시점 | 설명 |
|--------|----------|------|
| `correct` | 정답 시 | 상승하는 밝은 톤 (C5 → G5) |
| `incorrect` | 오답 시 | 짧은 낮은 톤 (G3 → C3) |
| `levelup` | 레벨업 시 | 상승 아르페지오 (C-E-G-C) |
| `click` | 버튼 클릭 시 | 짧은 틱 소리 (A5) |
| `complete` | 영역 완료 시 | 밝은 화음 (C-E-G) |

### 2. UI 업데이트

#### 헤더에 음소거 버튼 추가 (`index.html`)
```html
<button id="btn-mute" class="btn-icon" aria-label="소리 끄기">
  <span class="icon">🔊</span>
</button>
```

#### CSS 스타일 조정 (`css/style.css`)
- 헤더 오른쪽에 음소거 버튼과 레벨 배지가 함께 표시되도록 레이아웃 수정
- `min-width` 및 `gap` 속성 추가

### 3. 기존 코드 연동

#### `js/app.js`
- Sound 모듈 초기화
- 음소거 버튼 이벤트 핸들러 추가
- 주요 버튼 클릭 시 효과음 재생
- 정답/오답 시 효과음 재생

#### `js/level.js`
- 레벨업 이펙트 표시 시 효과음 재생
- 레벨업 닫기 버튼 클릭 시 효과음 재생

#### `js/diagnosis.js`
- 영역 완료 시 효과음 재생

### 4. Service Worker 업데이트 (`sw.js`)
- `sound.js` 캐시에 추가
- 캐시 버전 v4 → v5 업데이트

### 5. 테스트 페이지 제공 (`test-sound.html`)
- 개발자용 사운드 시스템 테스트 페이지
- 각 효과음을 개별적으로 테스트 가능
- 볼륨 및 음소거 설정 테스트 가능
- 현재 모드 표시 (MP3 파일 또는 Web Audio API 합성)

## 사용 방법

### 사용자 관점
1. 앱 실행 시 헤더 오른쪽 상단에 음소거 버튼 (🔊) 표시
2. 버튼 클릭으로 효과음 ON/OFF 전환
3. 설정은 자동으로 저장되어 다음 방문 시에도 유지

### 개발자 관점

#### 효과음 추가하기
1. MP3 파일을 `assets/sounds/` 폴더에 추가
   - `correct.mp3`
   - `incorrect.mp3`
   - `levelup.mp3`
   - `click.mp3`
   - `complete.mp3`

2. 파일이 없어도 자동으로 Web Audio API로 합성 효과음 생성

#### 코드에서 효과음 재생
```javascript
// 정답 효과음
Sound.play('correct');

// 오답 효과음
Sound.play('incorrect');

// 레벨업 효과음
Sound.play('levelup');

// 클릭 효과음
Sound.play('click');

// 완료 효과음
Sound.play('complete');
```

#### 볼륨 조절
```javascript
// 볼륨 설정 (0.0 - 1.0)
Sound.setVolume(0.5); // 50%

// 음소거 토글
const isMuted = Sound.toggleMute();

// 음소거 설정
Sound.setMute(true); // 음소거
Sound.setMute(false); // 음소거 해제
```

## 테스트 방법

### 1. 브라우저에서 테스트
```bash
# 로컬 서버 실행 (예: Python)
python -m http.server 8000

# 브라우저에서 접속
http://localhost:8000
```

### 2. 효과음 테스트 페이지
```bash
# 브라우저에서 접속
http://localhost:8000/test-sound.html
```

테스트 페이지에서:
- 각 효과음 버튼 클릭하여 소리 확인
- 볼륨 슬라이더로 볼륨 조절 테스트
- 음소거 체크박스로 음소거 테스트
- 상태 및 모드 확인

## 기술 세부사항

### Web Audio API 합성 효과음
실제 MP3 파일이 없는 경우, 다음과 같이 Web Audio API를 사용하여 실시간으로 효과음을 생성합니다:

```javascript
// 예: 정답 효과음 (상승하는 밝은 톤)
oscillator.type = 'sine';
oscillator.frequency.setValueAtTime(523.25, ctx.currentTime); // C5
oscillator.frequency.exponentialRampToValueAtTime(783.99, ctx.currentTime + 0.1); // G5
```

### 장점
1. **파일 크기 절약**: MP3 파일 없이도 작동
2. **오프라인 지원**: 네트워크 없이도 효과음 재생
3. **커스터마이징**: 코드로 효과음 조정 가능
4. **호환성**: 최신 브라우저에서 모두 지원

### 호환성
- Chrome/Edge: ✅ 완전 지원
- Firefox: ✅ 완전 지원
- Safari: ✅ 완전 지원 (단, 사용자 인터랙션 후 활성화)
- Mobile: ✅ iOS/Android 지원

## 파일 구조
```
soyu-learning-diagnosis/
├── assets/
│   └── sounds/
│       ├── README.md (업데이트됨)
│       ├── correct.mp3 (선택사항)
│       ├── incorrect.mp3 (선택사항)
│       ├── levelup.mp3 (선택사항)
│       ├── click.mp3 (선택사항)
│       └── complete.mp3 (선택사항)
├── js/
│   ├── sound.js (새로 생성)
│   ├── app.js (효과음 연동)
│   ├── level.js (효과음 연동)
│   └── diagnosis.js (효과음 연동)
├── css/
│   └── style.css (음소거 버튼 레이아웃 조정)
├── index.html (음소거 버튼 추가, sound.js 로드)
├── sw.js (캐시 업데이트)
└── test-sound.html (테스트 페이지)
```

## 다음 단계 (선택사항)

### MP3 파일 추가
무료 효과음을 다운로드하여 추가할 수 있습니다:
- [Freesound](https://freesound.org/) - CC0/CC-BY 라이선스
- [Mixkit](https://mixkit.co/free-sound-effects/) - 무료 효과음
- [Pixabay](https://pixabay.com/sound-effects/) - 무료 효과음

### 볼륨 조절 UI 추가
설정 페이지를 만들어 사용자가 볼륨을 조절할 수 있게 할 수 있습니다.

### 효과음 커스터마이징
`js/sound.js`의 `createSyntheticSound()` 메서드를 수정하여 효과음을 조정할 수 있습니다.

## 문제 해결

### 효과음이 재생되지 않음
1. 음소거 버튼이 켜져 있는지 확인
2. 브라우저 볼륨 확인
3. 콘솔에서 에러 메시지 확인
4. `test-sound.html`에서 테스트

### iOS Safari에서 소리가 안 남
iOS Safari는 사용자 인터랙션 이후에만 오디오 재생 가능합니다. 첫 버튼 클릭 후 정상 작동합니다.

### 효과음이 지연됨
Web Audio API 합성 효과음은 즉시 재생됩니다. MP3 파일을 사용하는 경우 프리로드가 완료되지 않았을 수 있습니다. `Sound.preloadSounds()`가 자동으로 호출되지만, 네트워크 상태에 따라 지연될 수 있습니다.

## 라이선스
Web Audio API 합성 효과음은 프로젝트 라이선스를 따릅니다.
외부 MP3 파일 사용 시 해당 파일의 라이선스를 준수해야 합니다.
