# 🏗️ 소유 학습 진단 앱 - 시스템 아키텍처

## 1. 시스템 개요

```
┌─────────────────────────────────────────────────────────────────┐
│                        PWA Application                          │
├─────────────────────────────────────────────────────────────────┤
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐        │
│  │   Home   │  │ Diagnosis │  │  Result  │  │Dashboard │        │
│  │   Page   │  │   Page   │  │   Page   │  │  (Parent) │        │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘        │
│       │             │             │             │              │
│  ┌────┴─────────────┴─────────────┴─────────────┴─────┐        │
│  │                    UI Layer (ui.js)                 │        │
│  └────────────────────────┬────────────────────────────┘        │
│                           │                                      │
│  ┌────────────────────────┴────────────────────────────┐        │
│  │              Application Logic Layer                │        │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌────────┐ │        │
│  │  │diagnosis │ │questions │ │  level   │ │character│ │        │
│  │  │   .js    │ │   .js    │ │   .js    │ │  .js   │ │        │
│  │  └──────────┘ └──────────┘ └──────────┘ └────────┘ │        │
│  └────────────────────────┬────────────────────────────┘        │
│                           │                                      │
│  ┌────────────────────────┴────────────────────────────┐        │
│  │                 Data Layer (storage.js)             │        │
│  │           localStorage + JSON Import/Export          │        │
│  └──────────────────────────────────────────────────────┘        │
│                                                                  │
│  ┌──────────────────────────────────────────────────────┐        │
│  │              Service Worker (sw.js)                  │        │
│  │              Offline Caching + PWA                    │        │
│  └──────────────────────────────────────────────────────┘        │
└─────────────────────────────────────────────────────────────────┘
```

## 2. 파일 구조

```
soyu-learning-diagnosis/
│
├── 📄 Core Files
│   ├── index.html              # SPA 진입점
│   ├── manifest.json           # PWA 매니페스트
│   └── sw.js                   # Service Worker
│
├── 🎨 css/
│   ├── style.css               # 메인 스타일
│   ├── theme.css               # 귀멸 테마 변수
│   └── animations.css          # 애니메이션
│
├── 📜 js/
│   ├── app.js                  # 앱 초기화, 라우팅
│   ├── storage.js              # 데이터 저장/불러오기
│   ├── ui.js                   # 화면 전환, 공통 UI
│   ├── character.js            # 하루 캐릭터 로직
│   ├── diagnosis.js            # 진단 흐름 제어
│   ├── questions.js            # 문제 로딩/관리
│   ├── level.js                # 레벨 시스템
│   └── dashboard.js            # 부모 대시보드
│
├── 📄 pages/
│   ├── home.html               # 홈 화면
│   ├── diagnosis.html          # 진단 화면
│   ├── result.html             # 결과 화면
│   └── dashboard.html          # 부모 대시보드
│
├── 📊 data/questions/
│   ├── vocabulary.json         # 어휘력
│   ├── self-efficacy.json      # 자기효능감
│   ├── reading.json            # 읽기 이해
│   ├── motivation.json         # 동기/정서
│   ├── grammar.json            # 문법 기초
│   └── strength.json           # 강점 발견
│
└── 🎨 assets/
    ├── images/                 # 캐릭터 이미지
    └── sounds/                 # 효과음 (선택)
```

## 3. 모듈 상세

### 3.1 app.js - 앱 코어
```javascript
// 역할: 앱 초기화, SPA 라우팅, 전역 상태 관리
const App = {
  init()              // 앱 초기화
  navigate(page)      // 페이지 전환
  getCurrentPage()    // 현재 페이지 반환
  registerServiceWorker() // PWA 등록
}
```

### 3.2 storage.js - 데이터 관리
```javascript
// 역할: localStorage CRUD, JSON 내보내기/가져오기
const Storage = {
  // Keys
  KEYS: {
    PROGRESS: 'soyu_progress',
    RESULTS: 'soyu_results',
    LEVEL: 'soyu_level'
  },

  // Methods
  save(key, data)     // 저장
  load(key)           // 불러오기
  clear()             // 초기화
  exportJSON()        // JSON 내보내기
  importJSON(data)    // JSON 가져오기
}
```

### 3.3 ui.js - UI 컨트롤러
```javascript
// 역할: 화면 전환, 모달, 토스트, 공통 컴포넌트
const UI = {
  showPage(pageId)           // 페이지 표시
  showModal(content)         // 모달 열기
  closeModal()               // 모달 닫기
  showToast(message, type)   // 토스트 알림
  updateProgress(percent)    // 진행률 바
  showLoading()              // 로딩 표시
  hideLoading()              // 로딩 숨김
}
```

### 3.4 character.js - 하루 캐릭터
```javascript
// 역할: 캐릭터 상태, 대사, 이미지 관리
const Character = {
  dialogues: {...}           // 대사 데이터

  speak(category)            // 대사 출력
  showEmotion(emotion)       // 감정 표현
  getImage(state)            // 이미지 반환
  celebrate()                // 축하 연출
}
```

### 3.5 diagnosis.js - 진단 엔진
```javascript
// 역할: 진단 흐름, 영역 관리, 결과 계산
const Diagnosis = {
  areas: [...]               // 진단 영역 목록
  currentArea: null          // 현재 영역

  start()                    // 진단 시작
  nextArea()                 // 다음 영역
  submitAnswer(answer)       // 답변 제출
  getResults()               // 결과 반환
  calculateScore(area)       // 점수 계산
}
```

### 3.6 questions.js - 문제 관리
```javascript
// 역할: 문제 로딩, 셔플, 난이도 조절
const Questions = {
  currentQuestion: null      // 현재 문제

  loadArea(areaId)           // 영역 문제 로드
  getNext()                  // 다음 문제
  shuffle()                  // 문제 셔플
  adjustDifficulty(correct)  // 난이도 조절
  getRemainingCount()        // 남은 문제 수
}
```

### 3.7 level.js - 레벨 시스템
```javascript
// 역할: 경험치, 레벨업, 형 관리
const Level = {
  levels: [
    { level: 1, name: '여우의 호흡 1형', exp: 0 },
    { level: 2, name: '여우의 호흡 2형', exp: 100 },
    // ...
  ],

  getCurrentLevel()          // 현재 레벨
  addExp(amount)             // 경험치 추가
  checkLevelUp()             // 레벨업 확인
  showLevelUpEffect()        // 레벨업 연출
}
```

### 3.8 dashboard.js - 부모 대시보드
```javascript
// 역할: 결과 분석, 차트, 내보내기
const Dashboard = {
  password: '7942',

  authenticate(input)        // 비밀번호 확인
  showResults()              // 결과 표시
  renderChart()              // Chart.js 렌더링
  analyzeStrengths()         // 강점 분석
  exportReport()             // 리포트 내보내기
}
```

## 4. 데이터 흐름

### 4.1 진단 흐름
```
[Start] → [Select Area] → [Load Questions] → [Answer Loop]
                                                   ↓
                                            [Submit Answer]
                                                   ↓
                                         [Check Correct] ─┬─ Yes → [Add EXP]
                                                          │         ↓
                                                          │  [Check Level Up]
                                                          │         ↓
                                                          └─ No → [Encourage]
                                                                    ↓
                                                          [Next Question]
                                                                    ↓
                                                    [Area Complete?] ─┬─ No → Loop
                                                                      │
                                                                      └─ Yes → [Next Area]
                                                                                  ↓
                                                                      [All Complete?] → [Result]
```

### 4.2 데이터 저장 구조
```javascript
// localStorage: soyu_progress
{
  currentArea: 'vocabulary',
  areasCompleted: ['vocabulary', 'self-efficacy'],
  questionIndex: 5,
  startTime: 1705XXX
}

// localStorage: soyu_results
{
  vocabulary: {
    correct: 15,
    total: 20,
    time: 300,
    answers: [0, 1, 0, 2, ...],
    difficulty: [3, 3, 4, 3, ...]
  },
  'self-efficacy': { ... }
}

// localStorage: soyu_level
{
  current: 3,
  exp: 250,
  name: '여우의 호흡 3형'
}
```

## 5. PWA 구조

### 5.1 manifest.json
```json
{
  "name": "소유 학습 진단",
  "short_name": "소유진단",
  "start_url": "/",
  "display": "standalone",
  "orientation": "portrait",
  "theme_color": "#9370DB",
  "background_color": "#1a1a2e",
  "icons": [...]
}
```

### 5.2 Service Worker 캐싱 전략
```javascript
// 캐시 우선 전략 (정적 자산)
const CACHE_NAME = 'soyu-diagnosis-v1';
const STATIC_ASSETS = [
  '/',
  '/index.html',
  '/css/style.css',
  '/js/app.js',
  // ...
];

// 네트워크 우선 전략 (문제 데이터)
// → 오프라인 시 캐시 사용
```

## 6. 보안 고려사항

### 6.1 데이터 보호
- localStorage만 사용 (서버 전송 없음)
- 부모 대시보드 비밀번호 보호
- 개인정보 최소화

### 6.2 콘텐츠 보안
- 외부 스크립트 제한
- CSP 헤더 권장
- 입력값 검증

## 7. 성능 최적화

### 7.1 로딩 최적화
- 코드 분할 (페이지별)
- 이미지 지연 로딩
- Service Worker 캐싱

### 7.2 런타임 최적화
- 문제 데이터 사전 로딩
- 애니메이션 GPU 가속
- 불필요한 리렌더링 방지

## 8. 확장 가능성

### 8.1 향후 추가 가능 기능
- 문제 콘텐츠 확장
- 학습 추천 시스템
- 진도 공유 (선택적)
- 다국어 지원

### 8.2 데이터 연동
- JSON 내보내기/가져오기
- CSV 리포트
- 인쇄 가능 결과지
